# Used only by Terraform and local AWS CLI calls (Runbook — Deploying To AWS).
# Copy to .credentials and fill in. .credentials is gitignored — never commit real values.
AWS_ACCESS_KEY_ID=AKIA...
AWS_SECRET_ACCESS_KEY=...
AWS_REGION=ap-south-1
